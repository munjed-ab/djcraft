## Setup

```
poetry install
poetry shell
```

## Example Usage

# Interactive mode:

```
cd dj-eleva
poetry run python main.py interactive
```

# Genaerate mode:

```
cd djcraft
poetry run python main.py generate newproject_config.yaml
```
