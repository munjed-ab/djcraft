from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    {% for app in apps %}
    path('{{app[0]}}/', include('{{ app[1] | safe }}.urls')),
    {% endfor %}
]