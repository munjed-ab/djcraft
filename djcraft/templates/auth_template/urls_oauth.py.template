# templates/auth_template/urls_oauth.py.template
# Basic URLs for OAuth authentication (using django-allauth)

from django.urls import path, include
from . import views

urlpatterns = [
    path('accounts/', include('allauth.urls')), # Include django-allauth URLs
    path('profile/', views.profile_view, name='profile'), # Example custom profile view
    # TODO: Add other URL patterns as needed
]

