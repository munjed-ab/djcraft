# templates/auth_template/urls_session.py.template
# Basic URLs for session authentication

from django.urls import path
from . import views

urlpatterns = [
    path('register/', views.register_view, name='register'),
    path('login/', views.login_view, name='login'),
    path('logout/', views.logout_view, name='logout'),
    # TODO: Add other URL patterns as needed (e.g., password reset)
]

